apiVersion: batch/v1
kind: Job
metadata:
  name: {{ include "cms-upload.name" . }}-hook
  labels:
    {{- include "cms-upload.labels" . | nindent 4 }}
  annotations: 
    {{- if .Values.hook.argocd.enabled }}
    {{- include "cms-upload-hook.argocd-syncwave" . | nindent 4 }}
    {{- else }}
    "helm.sh/hook": pre-install
    {{- end }}
spec:
  selector: {}
  template:
    metadata:
      name: {{ include "cms-upload.name" . }}-hook
    spec:
      serviceAccountName: {{ include "cms-upload.name" . }}-hook
      securityContext:
        {{- toYaml .Values.hook.podSecurityContext | nindent 8 }}
      containers:
        - name: {{ .Chart.Name }}
          securityContext:
            {{- toYaml .Values.hook.securityContext | nindent 12 }}
          image: "{{ .Values.hook.image.repository }}:{{ .Values.hook.image.tag }}"
          imagePullPolicy: {{ .Values.hook.image.pullPolicy }}
          env:
            - name: NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: HOST
              value: {{ .Values.hook.hostPattern }}
            - name: COUNT
              value: "{{ .Values.loop.count }}"
      restartPolicy: Never